server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/**
        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/order/**
